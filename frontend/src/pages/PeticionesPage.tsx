/**
 * Página de listado de peticiones (DESIGN.md 4.3). Placeholder de
 * scaffolding (T8): el consumo de `GET /api/peticiones`, el filtro por
 * estado y el botón de generación de datos de prueba se implementan en
 * T10/T12.
 */
function PeticionesPage() {
  return (
    <main className="placeholder">
      <h1>Peticiones</h1>
      <p>
        Listado de peticiones con filtro por estado. Se implementa en la
        tarea T10.
      </p>
    </main>
  )
}

export default PeticionesPage
