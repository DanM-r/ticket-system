/**
 * Página de login simulado (DESIGN.md 4.3). Placeholder de scaffolding
 * (T8): el formulario de `nombre` + selector de `rol` y la llamada a
 * `POST /api/auth/login` se implementan en T9.
 */
function LoginPage() {
  return (
    <main className="placeholder">
      <h1>Iniciar sesión</h1>
      <p>
        Formulario de login simulado (nombre + rol IT/Administración). Se
        implementa en la tarea T9.
      </p>
    </main>
  )
}

export default LoginPage
